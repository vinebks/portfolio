import styled from "styled-components";

const VerticalLine = styled.div`
  width: 1px;
  height: 100%;
  background-color: white;
`;

const Divider = () => <VerticalLine />;

export default Divider;
